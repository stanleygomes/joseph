spring:
  application:
    name: joseph
  datasource:
    url: jdbc:postgresql://localhost:5432/joseph
    username: joseph
    password: egypt-governor
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
